{% extends 'base.html.twig' %}

{% block title %}Edit Voitures
{% endblock %}

{% block body %}
	<div class="admin-content">
		{% include 'admin/header.html.twig' %}
		<div class="col-md-9 ">
			<div class="profil bg-secondary">
				<div class="profil-name">Bonjour
					<span class="fw-bold">{{ user.prenom }}</span>
				</div>
			</div>
			<div class="datas-content">
				<div class="add-data">
					<a href="{{ path('app_voitures_index') }}">Retour à la liste</a>
				</div>
				{{ include('voitures/_form.html.twig', {'button_label': 'Mise à jour'}) }}

				{{ include('voitures/_delete_form.html.twig') }}
			</div>
		</div>
	</div>

{% endblock %}
