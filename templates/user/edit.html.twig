{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<div class="admin-content">
		{% include 'admin/header.html.twig' %}
		<div class="col-md-9 ">
			<div class="profil bg-secondary">
				<div class="profil-name">Bonjour
					<span class="fw-bold">{{ userCo.prenom }}</span>
				</div>
			</div>
			<div class="datas-content">
				<div class="add-data">
					<a href="{{ path('app_user_index') }}">Retour à la liste</a>
				</div>

				{{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}


				{{ include('user/_delete_form.html.twig') }}

			</div>
		</div>
	</div>

{% endblock %}
